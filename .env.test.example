# Copy to .env.test for local integration test runs against a disposable Postgres.
# CI provides these same values via service containers in .github/workflows/ci.yml.
DATABASE_URL=postgres://postgres:postgres@localhost:5433/lab_platform_test
